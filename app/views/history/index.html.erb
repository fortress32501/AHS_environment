<h1> List of event attended </h1>
<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%# Display attendance history of user %> 
    <% Attendance.includes(:event).where(attendances: { user_id: current_user.id }).each do |attendance| %>
      <tr>
        <td><%= link_to Event.where(id: attendance.event_id).last.event_title, event_path(attendance.event_id) %></td>
        <td><%= Event.where(id: attendance.event_id).last.event_start.to_s(:short) %> to <%= Event.where(id: attendance.event_id).last.event_end.to_s(:short) %></td>
      </tr>
    <% end %>
  </tbody>
</table>