<nav class="navbar navbar-dark ahs-color justify-content-between">
    <!-- Logo -->
    <%= link_to "/", :class=>"navbar-brand ps-4 no-hover" do %> 
        <%= image_tag("ahs_logo.png", width: "70%", height: "70%") %>
    <% end %>  
    
    <!-- Navbar links -->
    <% if session.include? :user_id %>
      <ul class="nav justify-content-end pe-4">
        </li>
          <li class="nav-item">
            <%= button_to "Home", '/', :method => "get", :class=>"btn btn-default ahs-text" %>
          </li>
        <% if current_user.is_admin? %>
          <li class="nav-item">
            <%= button_to "Members", users_path, :method => "get", :class=>"btn btn-default ahs-text" %>
          </li>
          <li class="nav-item">
            <%= button_to "Events", events_path, :method => "get", :class=>"btn btn-default ahs-text" %>
          </li>
          <li class="nav-item">
            <%= button_to "Rankings", rankings_path, :method => "get", :class=>"btn btn-default ahs-text" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= button_to "Events", events_path, :method => "get", :class=>"btn btn-default ahs-text" %>
          </li>
        <% end %>
        <li class="nav-item">
          <%= button_to "Sign Out", session_path(current_user.id), method:"delete", :class=>"btn btn-default ahs-text" %>
        </li>
        <li class="nav-item">
          <%= link_to accounts_path, :class=>"btn btn-default ahs-text", :id=>"account" do %>
            <i class="fa-solid fa-circle-user fa-2xl" aria-hidden="true"></i>
          <% end %>
        </li>
      </ul> 
    <% end %> 
</nav>